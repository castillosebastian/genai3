{
  "schema": 1,
  "description": "Extract entities related to a specified topic from the supplied input text. Returns the entities and the source text",
  "execution_settings": {
    "default": {
      "max_tokens": 256,
      "temperature": 0.0,
      "top_p": 0.1,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "ask",
      "description": "The user question from which the entities are to be extracted",
      "default": "",
      "is_required": true
    }
  ]
}
